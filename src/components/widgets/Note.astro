---
// import { Icon } from 'astro-icon';

const iClass = 'text-md mx-auto max-w-6xl px-4 py-4 text-center font-medium sm:px-6';
const oClass = 'bg-primary-800 dark:bg-primary-900 text-white';

const { innerClass = iClass, outterClass = oClass } = Astro.props;
---

<section class={outterClass}>
	<div class={innerClass}>
			<slot />
	</div>
</section>
